# 医务工作者互助平台 UI 设计规范

| 版本 | v1.1 |
| 日期 | 2026-08-26 |
| 适用 | 需求页、留言板页及其子组件 |

## 1. 设计令牌（Design Tokens）

```css
/* 色彩 */
--c-primary: #0F6E56;        /* 医疗青绿：导航/主按钮/选中态/FAB */
--c-primary-bg: #E1F5EE;     /* 主色浅底 */
--c-bg-page: #F6F7F9;        /* 页面背景 */
--c-bg-card: #FFFFFF;        /* 卡片背景 */
--c-text: #1F2A26;           /* 主文本 */
--c-text-2: #7A8B87;         /* 次文本 */
--c-text-3: #9AA8A4;         /* 辅助文本 */
--c-fee: #993C1D;            /* 价格强调（暖褐） */
--c-ad-bg: #FDFCF7;          /* 广告位底色 */
--c-ad-border: #F0E6C8;      /* 广告位边框 */

/* 类目标签（值班/病例指导两类型 + 预留扩展） */
--tag-shift: bg #E1F5EE / text #0F6E56;   /* 值班：青绿 */
--tag-case:  bg #E6F1FB / text #185FA5;   /* 病例：蓝 */

/* 状态徽标 */
待接单=灰(#F1EFE8/#5F5E5A) · 申请中=蓝(#E6F1FB/#185FA5) ·
已确认=青绿(#E1F5EE/#0F6E56) · 履约中=琥珀(#FAEEDA/#854F0B) ·
已完成=灰绿(#EDF1EF/#5F6E6A) · 争议中=红(#FCEBEB/#A32D2D)

/* 字号（三级） */
标题 32rpx/500 · 正文 26rpx/400 · 辅助 24rpx/400

/* 形状 */
卡片圆角 16rpx · 标签圆角 8rpx · 筛选胶囊圆角 999rpx · FAB 104rpx 圆形
```

## 2. 信息架构（5-tab，D11）

```
需求 / 留言板 / 发布（居中） / 消息 / 我的
```

## 3. 需求页（D12）

```
┌────────────────────────────┐
│ [搜索栏] 搜索标题、科室、医院… │  搜索：标题/科室/医院名模糊
├────────────────────────────┤
│ 综合▾ 类型▾ 科室▾ 时间▾ 价格▾ ⋁│  筛选导航栏（吸顶）
│ (已选条件胶囊回显区，可单个清除) │
├────────────────────────────┤
│ [广告位1] 需求流第2条 · 地域定向│
│ ┌────────────────────────┐ │
│ │[值班]  求值8/28夜班… 待接单│ │  需求卡片
│ │吉大一院 · 8/28 18:00-次日8:00│ │  类目标签+状态右上角
│ │￥200                     │ │  价格暖褐强调
│ └────────────────────────┘ │
└────────────────────────────┘
```

**筛选器规格：**

| 筛选维度 | 控件 | 选项 |
|---|---|---|
| 综合（排序） | 下拉单选 | 最新发布 / 价格从低到高 / 价格从高到低 / 时间临近 |
| 类型 | 下拉单选 | 全部 / 值班 / 病例指导 |
| 科室 | 下拉单选（标准科室字典） | 全部 + 一级科室 + 亚专科 |
| 时间 | 下拉单选 | 全部 / 近24小时 / 近7天 / 近30天 / 自定义起止 |
| 价格 | 下拉单选 | 全部 / 面议 / ￥0-100 / 100-300 / 300+ / 自定义区间 |
| ⋁ 筛选抽屉 | 半屏抽屉 | 医院搜索（**模糊+别名匹配**）、省市级联、价格区间输入 |

服务端对应 `dealing.list` 参数：`keyword, sort, category, department, timeFrom, timeTo, feeMin, feeMax, hospitalId, province, city`。

## 4. 留言板页（D13）

```
┌────────────────────────────┐
│ [话题：全部▾] [时间：近7天▾]   │  筛选胶囊行
│ [省市：长春市▾（跟随浏览地域）]  │
├────────────────────────────┤
│ ┌────────────────────────┐ │
│ │(李) 李医生·吉大一院  1小时前 │ │  帖子：头像+昵称+医院
│ │#规培心得#                │ │  话题标签
│ │第一个月夜班的三个教训…      │ │  正文两行截断
│ │赞 36 · 评论 12 · 回复     │ │  互动行
│ └────────────────────────┘ │
│ [广告位2] 帖子流内嵌横条      │
└────────────────────────────┘
发帖入口：页面右上角"发帖"按钮（或 FAB，与需求页区分：文案"发帖"）
```

**话题字典（初始，可运营扩展）：** 规培心得 / 病例讨论 / 求助提问 / 经验分享 / 考研考博 / 招聘信息 / 闲聊灌水

**合规双闸门：**
1. 发帖/评论/回复提交前 `msgSecCheck` 文本检测，违禁（政治敏感/黄赌毒/暴力等）直接拦截并提示；
2. 图片 `imgSecCheck`；举报入口全量覆盖，举报成立下架 + 账号处置；
3. 运营侧敏感词黑名单二次过滤（字典可配置）。

## 5. 广告位（D14）

| 位置 | 形态 | 定向字段 |
|---|---|---|
| 需求流第 2 条 | 原生卡片（浅金底 + "广告"角标） | province / city / hospital_id |
| 留言板帖子流 | 内嵌横条（第 4 条后） | 同上 |

```js
// ads 集合结构（Pilot 期不投放，仅预留）
{
  _id, title, image_fileID, jump_type(none/web/page), jump_value,
  province, city, hospital_id,     // 地域定向：null=全国
  slot: 'dealing_feed_1' | 'board_feed_1',
  status: active/disabled,
  start_at, end_at, priority, created_at
}
```

投放逻辑（服务端注入信息流）：按用户浏览地域（browsingHospital 所属省市）匹配 `ads`，插到信息流固定位；无匹配不展示。上线广告前需补充：广告主资质、流量主开通（微信流量主需小程序累计独立访客达标）、广告内容审核。

## 6. 组件清单

| 组件 | 用途 |
|---|---|
| `filter-bar` | 需求页筛选导航栏（下拉+抽屉） |
| `dealing-card` | 需求卡片（标签/状态/价格/医院/时间） |
| `post-card` | 留言板帖子卡片（头像/话题/互动行） |
| `status-badge` | 状态徽标（六色） |
| `ad-slot` | 广告位容器（地域定向加载，无广告透明） |
| `hospital-picker` | 医院搜索选择器（模糊+别名） |
| `dept-picker` | 标准科室选择器（字典驱动） |
